# dependencies
node_modules

# build output
.next
out
tsconfig.tsbuildinfo
*.log
server.log

# Prisma generated client (recreate with `npx prisma generate`)
src/generated/

# env
.env*
!.env.example

# local tooling artifacts
.playwright-mcp
.opencode/
.DS_Store

# regenerable design references (scripts/crawl/measure.mjs + shoot-rebuilt.mjs)
design/
scripts/crawl/*.txt

# admin lane (drafts, history snapshots, local data, uploads, tooling)
content/**/*.draft.json
content/.history/
data/
*.sqlite*
public/uploads/
.openchamber/

# deepwork session state (local only)
.slim/deepwork/
